
import {Meteor} from 'meteor/meteor';

export default function () {

  // Meteor.puplish('partnersList', function partnersList() {
  //   const {MANAGER, SUPERADMIN} = CONST.USERS.ROLES;

  //   if (!Users.hasRole(this.userId, MANAGER, SUPERADMIN)) {
  //     return this.error(new Meteor.Error(403, 'No access allowed for this'));
  //   }

  //   return Partners.find({});
  // });
}
